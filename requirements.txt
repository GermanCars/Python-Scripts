# yt-dlp with its recommended default extras (mutagen, certifi, brotli, websockets).
# Keep it up to date — it breaks whenever sites change.
# Easy way:  python yt_downloader.py update
yt-dlp[default]>=2024.10.0
